const baseConfig = {
    // style
    slideWidth: '4em',
    slideGap: '10px',
    slideShadows: true,

    centeredItems: true,

    grabCursor: true,

    // drag
    enableDragging: true,
    slideChangeDragFactor: 1,
    disableInteractionWhileDragging: true,

    // interaction
    enableSlideInteraction: true,
    clickToMoveToSlide: true, // will disable slide interaction on all slides besides the active one
};

export default baseConfig;